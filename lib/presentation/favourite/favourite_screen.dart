import 'package:flutter/material.dart';

class FavouritScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("FavouritScreen")),
    );
  }
}
